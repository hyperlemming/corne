# Enable display
CONFIG_ZMK_DISPLAY=y

# Split configuration - these MUST be set
CONFIG_ZMK_SPLIT=y
CONFIG_ZMK_SPLIT_ROLE_CENTRAL=y

# BLE configuration
CONFIG_ZMK_BLE=y
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# USB support
CONFIG_ZMK_USB=y

# Logging (disable for production)
# CONFIG_ZMK_USB_LOGGING=y
